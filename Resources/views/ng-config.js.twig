.config(function($mdThemingProvider, $controllerProvider, $httpProvider) {
	$mdThemingProvider.theme('default')
		.primaryPalette('teal', {'default': '900'})
		.warnPalette('red')
		.accentPalette('light-green', {
			'default': '600',
			'hue-1': '700',
			'hue-2': '900',
			'hue-3': 'A700'
		})
		.backgroundPalette('grey', {'default': '900'})
		.dark();
	app.controllerProvider = $controllerProvider;

	// Do not encode POST data in Angular JSON format, but use jquery POST parameters format (query string)
	$httpProvider.defaults.transformRequest = function(data){
		if (data === undefined) return data;
		return $.param(data);
	}
	$httpProvider.defaults.headers.post['Content-Type'] = 'application/x-www-form-urlencoded; charset=UTF-8';
})
